# 功能遷移狀態追蹤 (Migration Status)

本文件整理自各 OpenSpec migration notes，追蹤自訂功能回歸 Blowfish 原生架構後的狀態。

## ✅ 已完成 (Completed)

| 功能項目 | 說明 | 解決方案 |
| :--- | :--- | :--- |
| **Taxonomy Badge Colors**<br>(分類/標籤顏色區分) | 分類顯示綠色系，標籤顯示藍色系。 | 已透過 `restore-taxonomy-badge-colors` 實作純 CSS 方案 (`taxonomy-badges.css`) 恢復。 |
| **Card Visual Styling**<br>(卡片視覺樣式) | 卡片的毛玻璃背景、邊框樣式與 Hover 效果。 | 已透過 `restore-card-styling` 實作，將樣式遷移至 `assets/css/custom/article-cards.css`。 |
| **Badge i18n**<br>(Badge 內翻譯) | Badge 顯示文字的自動 i18n 轉換。 | 已透過 `restore-badge-i18n` 實作，改採 Hugo 原生 Taxonomy Term `_index.md` 機制處理多語標題，並建立自動化腳本確保同步。 |

## ⏳ 等待上游更新 (Pending Upstream)

| 功能項目 | 說明 | 當前狀態/計畫 |
| :--- | :--- | :--- |
| **Summary Line Clamp**<br>(摘要行數限制) | 列表頁摘要文字的自動截斷 (`line-clamp`)。 | **已在上游實作**。Blowfish PR #2714 已實作 `compactSummary` 參數 (預設啟用)。待上游合併發布後升級主題即可。 |

## 🚧 待處理 (Pending)

| 功能項目 | 說明 | 當前狀態/計畫 |
| :--- | :--- | :--- |

## 🚫 不影響 / 不需處理 (No Action Needed)

| 功能項目 | 說明 | 原因 |
| :--- | :--- | :--- |
| **Custom Image Handling**<br>(自訂圖片處理) | 舊版使用 CSS `background-image` 顯示縮圖。 | **已棄用**。改用 Blowfish 原生 `<img>` 標籤以獲得更佳效能 (Lazy loading/Async decoding)。 |
| **Badge Dict/Type Params**<br>(Badge 參數支援) | `badge.html` 接收複雜 Dict 或 Type 參數。 | **已棄用**。經分析專案內未實際使用此功能，移除後無負面影響。 |
